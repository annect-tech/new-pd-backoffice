import { Box, Typography } from "@mui/material";

const Cidades = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Cidades</Typography>
      <Typography variant="body1">Página de Cidades</Typography>
    </Box>
  );
};

export default Cidades;

