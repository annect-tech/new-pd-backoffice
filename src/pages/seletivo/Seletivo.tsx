import { Box, Typography } from "@mui/material";

const Seletivo = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Seletivo</Typography>
      <Typography variant="body1">Página de Seletivo</Typography>
    </Box>
  );
};

export default Seletivo;

