import { Box, Typography } from "@mui/material";

const Usuarios = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Usuários</Typography>
      <Typography variant="body1">Página de Usuários</Typography>
    </Box>
  );
};

export default Usuarios;

