import { Box, Typography } from "@mui/material";

const ListaPresenca = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Lista de Presença</Typography>
      <Typography variant="body1">Página de Lista de Presença</Typography>
    </Box>
  );
};

export default ListaPresenca;

