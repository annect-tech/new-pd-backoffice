import { Box, Typography } from "@mui/material";

const DadosAlunos = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Dados de Alunos</Typography>
      <Typography variant="body1">Página de Dados de Alunos</Typography>
    </Box>
  );
};

export default DadosAlunos;

