import { Box, Typography } from "@mui/material";

const Contratos = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Contratos</Typography>
      <Typography variant="body1">Página de Contratos</Typography>
    </Box>
  );
};

export default Contratos;

